import React from 'react';

// internal import
import SearchText from '../../Components/Search/SearchText';

const Search = () => {
  return (
    <div>
      <SearchText />
    </div>
  );
};

export default Search;
